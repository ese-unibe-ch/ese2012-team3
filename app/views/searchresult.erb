<%
   @title = @LANG["SEARCH_RESULT"]
   @current_nav_item = :marketplace
%>

<h2><%=@LANG["SEARCH_RESULT"]%></h2>

<%= erb :_item_list, :locals => {
                       :items => @found_items,
                       :offers => @found_offers,
                       :show_owner => true
} %>
